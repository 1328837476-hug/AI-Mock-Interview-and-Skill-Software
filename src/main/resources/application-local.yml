spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_interview?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root         # 改为你本机MySQL用户名
    password: 203619      # 改为你本机MySQL密码
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  data:
    redis:
      host: localhost
      port: 6379
      password:            # 本地Redis无密码留空即可
      database: 0

  # 本地环境关闭缓存，方便调试
  thymeleaf:
    cache: false

logging:
  level:
    com.supidan: debug     # 本地开启debug日志
    org.springframework.security: debug